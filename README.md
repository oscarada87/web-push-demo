# Demo for the Web Notification

This is the web notification demo code

## Usage
To launch backend:

```bash=
cd backend
npm install
node index.js
```

To launch frontend:

```bash=
cd frontend
npm install -g live-server
live-server
```
